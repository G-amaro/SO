#ifndef SERVIDOR_H
#define SERVIDOR_H

/**
 * @brief Função principal da aplicação servidor.
 * 
 * Esta função é o ponto de entrada do programa servidor. 
 * Trata os argumentos da linha de comandos e inicia os serviços do servidor.
 * 
 * @param argc Número de argumentos da linha de comandos.
 * @param argv Vetor de strings contendo os argumentos.
 * @return int Código de saída da aplicação (0 para sucesso, outro valor em caso de erro).
 */

int main(int argc, char *argv[]);

#endif