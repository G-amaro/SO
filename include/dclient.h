#ifndef CLIENT_H
#define CLIENT_H

/**
 * @brief Função principal da aplicação cliente.
 * 
 * Esta função é o ponto de entrada do programa cliente. 
 * Processa os argumentos da linha de comandos e inicia a lógica principal.
 * 
 * @param argc Número de argumentos passados pela linha de comandos.
 * @param argv Vetor de strings contendo os argumentos.
 * @return int Código de saída da aplicação (0 para sucesso, outro valor para erro).
 */

int main(int argc, char *argv[]);

#endif 
